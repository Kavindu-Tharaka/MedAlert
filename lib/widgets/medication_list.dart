import 'package:flutter/material.dart';

class MedicationList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      child: ListTile(
        title: Text("Medication data"),
      ),
    );
  }
}